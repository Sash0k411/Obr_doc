= render partial: "shared/breadcrumbs", locals: { items: @category.parents_with_self }

.container-fluid
  %h1
    = @category.title
  %p
    = @category.description

  - documents = Document.where(category_id: @category.subtree_ids)
  = render partial: "shared/same_documents", locals: { documents: documents }