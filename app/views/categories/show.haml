= render partial: "shared/breadcrumbs", locals: { items: @category.parents }

%h1
  = @category.title
%p
  = @category.description

%ul
  - Document.where(category_id: @category.subtree_ids).each do |document|
    %li
      %a{href: document.url}
        = document.title